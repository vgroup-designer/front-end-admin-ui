{
  "name": "shopify-admin-agent",
  "version": "0.1.0",
  "description": "Operations agent for Shopify admin tasks, built on the Claude Agent SDK",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "web": "node src/server.js",
    "web:dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.274",
    "dotenv": "^16.4.5",
    "zod": "^4.0.0"
  }
}
